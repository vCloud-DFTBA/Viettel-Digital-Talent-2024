FROM mysql:latest

# Thiết lập các biến môi trường cần thiết
ENV MYSQL_ROOT_PASSWORD=Minhtuan03
ENV MYSQL_DATABASE=vdt
ENV MYSQL_USER=root
ENV MYSQL_PASSWORD=Minhtuan03

# Expose the default MySQL port
EXPOSE 3306

# Chạy MySQL server
CMD ["mysqld"]
